[module/systray]
type = internal/tray

tray-size= 66%
tray-spacing = 2px
tray-padding = 2px
format-background = ${colors.background-alt-2}
tray-background = ${colors.background-alt-2}
tray-foreground = ${colors.foreground}
